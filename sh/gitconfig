########################################################
#----------------- GIT CONFIG TEMPLATE ----------------#
########################################################

[alias]
	ci = commit
	co = checkout
	mg = merge
	pp = !git pull -- && git push --
	st = status
	unstage = reset HEAD --
	who = shortlog -s --
	rmdel = !git rm $(git ls-files --deleted)
	undo = !git reset --hard && git rmdel && git clean -f
[push]
	default = matching
[color]
	ui = true
